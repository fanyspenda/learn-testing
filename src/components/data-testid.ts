const component = {
	header: "header",
	card: "card",
};

export const headerTestId = {
	title: `${component.header}-title`,
	todoInput: `${component.header}-todoInput`,
	todoSubmit: `${component.header}-todoSubmit`,
};

export const cardTestId = {
	todoButton: `${component.card}-todoButton`,
	doingButton: `${component.card}-doingButton`,
	doneButton: `${component.card}-doneButton`,
	deleteButton: `${component.card}-deleteButton`,
};
